Criar um programa que realiza a leitura via std::cin de palavras (sem espaço, com
std::string, std::vector). Elaborar um menu contendo as seguintes opções:

UFxC String Store V.0
    1. Insert string
    2. Print index and string
    3. Search string (literal)
    4. Search substrings
    5. Remove string (by index)
    6. Remove by substrings (all occurrences)
    0. Quit